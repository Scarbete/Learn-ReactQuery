import { TodoForm, TodosList } from '@/features/todo'

const TodoComponent = () => {
    return (
        <section>
            <TodoForm/>
            <TodosList/>
        </section>
    )
}

export default TodoComponent